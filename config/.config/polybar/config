[colors]
alert=#bd2c40
primary=#6272a4
foreground=#ffffff
foreground-alt=#44475a
background=#282a36

[bar/main]
cursor-click=pointer
cursor-scroll=ns-resize
fixed-center=true
font-0=CascadiaCode:style=Regular:pixelsize=10;3
height=21
line-color=#f00
line-size=3
module-margin-left=0
module-margin-right=2
modules-center = 
modules-left = ewmh xwindow
modules-right = volume ip date
padding-left = 0
padding-right = 2
tray-padding = 1
tray-position=right
width = 100%
enable-ipc = true
background = ${colors.background}

[module/ewmh]
type = internal/xworkspaces

pin-workspaces = false
enable-click = true
enable-scroll = true
reverse-scroll = true

format = <label-state>
label-monitor = %name%

label-active = %name%
label-active-foreground = ${colors.foreground}
label-active-background = ${colors.primary}
label-active-padding = 1

label-occupied = %name%
label-occupied-background = ${colors.background}
label-occupied-padding = 1

label-urgent = %name%
label-urgent-foreground = ${colors.foreground}
label-urgent-background = ${colors.alert}
label-urgent-padding = 1

label-empty =
label-empty-foreground = ${colors.foreground-alt}
label-empty-padding = 1
format-foreground = ${colors.foreground}
format-background = ${colors.background}

[module/ip]
type = custom/script
exec = /home/theocodes/dotfiles/scripts/polybarscripts ip
format = IP <label>
interval = 30

[module/volume]
type = custom/script
exec = /home/theocodes/dotfiles/scripts/polybarscripts volume
format = VOL <label>
interval = 5

[module/xwindow]
label=" %title:0:80:...%"
type=internal/xwindow

[module/date]
type = internal/date
interval = 1
date = %a %d %B %Y
date-alt = "%d-%m-%Y"

time = %H:%M:%S
time-alt = %H:%M
format-prefix = 
format-prefix-foreground = #fff
label = " %date% %time%"

[module/pulseaudio]
type = internal/pulseaudio

# ; Sink to be used, if it exists (find using `pacmd list-sinks`, name field)
# ; If not, uses default sink
# sink = alsa_output.pci-0000_12_00.3.analog-stereo
sink = alsa_output.usb-SteelSeries_SteelSeries_Arctis_1_Wireless-00.analog-stereo

# ; Use PA_VOLUME_UI_MAX (~153%) if true, or PA_VOLUME_NORM (100%) if false
# ; Default: true
# use-ui-max = true
format-volume = <ramp-volume> <label-volume>
label-muted = 🔇 muted
click-right = pavucontrol

# ; Interval for volume increase/decrease (in percent points)
# ; Default: 5
interval = 5
